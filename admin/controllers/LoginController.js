/*global define*/
'use strict';

define(['angular', 'app', 'createController'], function (angular, app, createController) {

	return app.controller("LoginController", createController(function ($scope) {
	
	}));
	
});