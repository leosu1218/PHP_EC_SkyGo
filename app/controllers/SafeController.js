/*global define*/
'use strict';

define(['angular', 'app', 'jquery', 'slick'], function (angular, app, $, slick) {

	return app.controller("SafeController", function ($scope, $log, $q, $timeout, $http, $interval) {
		
            
	});	
});