/*global define*/
'use strict';

define(['angular', 'app', 'jquery', 'slick'], function (angular, app, $, slick) {

	return app.controller("ShoppingGuideController", function ($scope, $log, $q, $timeout, $http, $interval) {
		console.log('ShoppingGuideController working');
		
		
            
	});	
});