/*global define*/
'use strict';

define(['angular', 'app', 'jquery', 'slick'], function (angular, app, $, slick) {

	return app.controller("DeliveryProblemController", function ($scope, $log, $q, $timeout, $http, $interval) {
		console.log('DeliveryProblemController working');
		
		
            
	});	
});