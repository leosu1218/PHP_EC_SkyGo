/*global define*/
'use strict';

define(['angular', 'app', 'jquery', 'slick'], function (angular, app, $, slick) {

	return app.controller("PrivacyController", function ($scope, $log, $q, $timeout, $http, $interval) {
		console.log('PrivacyController working');
		
		
            
	});	
});