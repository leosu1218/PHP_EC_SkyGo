/*global define*/
'use strict';

define(['angular', 'app', 'createController'], function (angular, app, createController) {

	return app.controller("HomeController", createController(function ($scope) {
	}));
	
});